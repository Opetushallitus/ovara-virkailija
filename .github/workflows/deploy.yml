name: Dummy Placeholder Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  no_op_job:
    runs-on: ubuntu-latest
    steps:
      - name: No-op Step
        run: echo "This is a no-op job, placeholder in the main branch for actual deploy workflow."
